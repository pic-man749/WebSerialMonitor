{
  "name": "web-serial-monitor",
  "version": "0.1.0",
  "description": "Browser-based serial communication monitoring tool using Web Serial API",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
